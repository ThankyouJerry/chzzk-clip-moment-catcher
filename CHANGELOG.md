# 변경 이력

## 다음 릴리스

### 분석 정확성과 대용량 안정성

- 시스템 메시지뿐 아니라 내용이 없는 빈 메시지도 채팅 밀도와 분위기 분석 근거에서 제외합니다.
- 분위기 변화는 인접한 두 구간 모두 정서 근거 메시지 2개 이상, 근거 커버리지 3% 이상일 때만 표시합니다.
- 시간 구간을 최대 20,000개로 제한하고, 지나치게 짧은 분석 간격에는 필요한 최소 간격을 안내합니다.
- 분위기 타임라인을 구간마다 전체 채팅을 반복 검색하지 않고 한 번의 그룹 집계로 계산합니다.
- 대용량 CSV 헤더 검증이 파일 전체가 아닌 첫 줄만 읽도록 변경했습니다.
- 편집 작업표 CSV가 밀리초 시각과 숫자형 초 값을 함께 보존합니다.
- 분석 가능한 일반 채팅이 없으면 `이벤트 없음`이 아니라 근거 부족 상태로 표시합니다.

## 1.2.2 - 2026-08-06

### 안정성과 집계 정확성

- 시스템 메시지만 있는 CSV의 정서·반응 강도 분석 결과도 일관된 빈 데이터 구조로 반환해 결과 화면이 종료되지 않게 수정했습니다.
- 분석할 일반 채팅이 없으면 정서 방향과 평균 반응 강도를 `근거 부족`으로 표시합니다.
- 한 구간을 사이에 둔 급증 후보를 하나의 사건으로 병합할 때, 사건 범위의 채팅 수·기준선·참여자·신뢰도를 동일한 연속 구간에서 계산합니다.
- 병합 사건 사이 구간도 타임라인에서 같은 사건으로 표시합니다.

## 1.2.1 - 2026-08-05

### CSV 호환성과 표시 정확성

- `chzzk-chat-exporter`의 이전 영문 열 형식과 `_part001.csv` 분할 파일을 자동 인식합니다.
- 이전 exporter의 ISO 재생시간을 VOD 경과시간으로 변환합니다.
- 키워드 분석의 사건·피크 수치를 채팅 수가 아닌 키워드 출현 횟수로 명확히 표시합니다.
- 참여자 관련 수치가 닉네임 기준 추정값임을 UI와 편집 작업표에 표시합니다.
- 15초 피크를 반개방 시간 구간으로 계산하고 사건 종료 경계의 다음 구간 채팅이 중복 포함되지 않게 수정했습니다.

### 문서

- README를 일반 사용자 중심의 다운로드, CSV 준비, 분석 선택, 편집 연동 순서로 정리했습니다.
- 영상·음성 비분석, 마커 전용 XML, CSV 마지막 채팅 시각 제한, 신뢰도와 개인정보·저작권 한계를 명시했습니다.
- 개발 환경과 기여 절차는 `CONTRIBUTING.md`로 분리했습니다.

## 1.2.0

### 분석 정확도

- CSV 필수 열, 시간 형식, 분할 파일 순서와 스키마를 엄격하게 검증합니다.
- 빈 채팅과 시스템 메시지를 분석에서 분리하고 사용자 ID와 커스텀 이모티콘을 보존합니다.
- 채팅 급증 기준을 전체 평균 대신 구간별 중앙값과 변동성 기반의 로컬 기준선으로 변경했습니다.
- 감도를 높일수록 작은 급증도 찾도록 방향을 바로잡고, 짧거나 균일한 데이터에는 거짓 이벤트를 만들지 않습니다.
- 밀집한 15초 원본 채팅 창에서 실제 피크를 계산하고 이벤트의 상승 배수, 신뢰도, 참여자 수를 함께 표시합니다.

### 정서와 텍스트

- 정서 방향과 반응 강도를 분리하고 부정 표현, 반복 반응, 이모지와 커스텀 이모티콘을 처리합니다.
- 데이터가 없는 구간은 중립으로 채우지 않고 알 수 없는 구간으로 유지합니다.
- 키워드가 포함된 메시지 수와 실제 출현 횟수를 구분합니다.
- 워드클라우드에서 URL, 숫자, 한 글자 토큰을 제외하고 반복 반응어를 정규화합니다.

### 편집 연동

- 실제 피크를 기준으로 프리롤과 포스트롤을 적용하며 0초와 CSV의 마지막 채팅 시각을 벗어나지 않게 제한합니다.
- 편집 작업표 CSV, Premiere 교환 XML, Final Cut FCPXML 내보내기를 제공합니다.
- 23.976, 29.97, 59.94를 포함한 주요 편집 프레임레이트를 지원합니다.

### 앱과 배포

- 긴 분석을 작업 스레드로 옮기고 진행 상태, 중복 실행 방지, 결과 이력 탭을 추가했습니다.
- 결과 화면 전체를 스크롤할 수 있게 하고 증거 표와 전체 타임라인을 함께 표시합니다.
- 단일 버전 소스, 단일 진입점, 단일 PyInstaller 설정으로 정리했습니다.
- Ubuntu, Windows, macOS 테스트와 Windows, macOS Intel, Apple Silicon 빌드를 Actions에 추가했습니다.
- macOS 패키지는 메타데이터 정리, ad-hoc 서명 검증, 실행 스모크 테스트를 거쳐 생성됩니다.

## 이전 버전

- [1.1.0](https://github.com/ThankyouJerry/chzzk-clip-moment-catcher/compare/v1.0.3...v1.1.0)
- [1.0.3](https://github.com/ThankyouJerry/chzzk-clip-moment-catcher/compare/v1.0.2...v1.0.3)
- [1.0.2](https://github.com/ThankyouJerry/chzzk-clip-moment-catcher/compare/v1.0.1...v1.0.2)
- [1.0.1](https://github.com/ThankyouJerry/chzzk-clip-moment-catcher/compare/v1.0.0...v1.0.1)
- [1.0.0](https://github.com/ThankyouJerry/chzzk-clip-moment-catcher/releases/tag/v1.0.0)
